name: SDXI driver CI

on:
  pull_request:
  push:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        arch: [arm64, m68k, x86_64, s390]
        cc: [gcc, clang]
        include:
          - sparse: fatal
          - cc: clang
            sparse: off
          - arch: [arm64, s390, m68k]
            sparse: warn # various known issues with these arches
        exclude:
          - arch: m68k
            cc: clang
    steps:
      - uses: actions/checkout@v4
      - name: Create build environment image
        run: |
          ci-tools/setup buildenv
      - name: Check
        run: |
          ci-tools/sdxi-quick-check --sparse=${{ matrix.sparse }} buildenv ${{ matrix.arch }} ${{ matrix.cc }}
